# Mobile AI: Camera RGB Normalization Pipeline
**Target:** Android / iOS Client Devices

### 💡 Suggested Prompts:
- *"Vectorize `normalize_rgb` using Arm NEON de-interleaving loads (`vld3q_u8`) and FP32 conversion vectors to reduce energy usage."*
