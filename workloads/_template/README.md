# Workload Name: [Brief Description of Workload]

This workload demonstrates a baseline unoptimized implementation of [workload algorithm/operator], serving as a target for Arm hardware-specific optimizations (e.g., NEON, KleidiAI, SVE, or Compute Library).

### 💡 Suggested Prompts for IDE Chat:
- *"Profile `kernel.cpp` on the remote Arm Tau T2A sandbox and identify compiler auto-vectorization opportunities."*
- *"Refactor the baseline kernel in `kernel.cpp` using [NEON / KleidiAI / other optimizations] and benchmark the throughput."*
